h3.text-center {{title}}
i.page__back.fa.fa-reply.fa-2x(ng-click="goBack()")
.page__content
	form
		.right-column
			.form-group
				label(for="InputName") Имя
				input.form-control(type="text" id="InputName" name="InputName" placeholder="Имя контакта" ng-model="user.name" value="{{user.name}}")
			.form-group
				label(for="InputLastName") Фамилия
				input.form-control(type="text" id="InputLastName" name="InputLastName" placeholder="Фамилия контакта" ng-model="user.lastname" value="{{user.lastname}}")
			b Контактная информация
			.form-group
				.input-group.contact__input
					.input-group-addon
						i.fa.fa-envelope
					input.form-control(type="text" id="InputMail" placeholder="example@gmail.com" ng-model="user.mail" value="{{user.mail}}")
			.form-group
				.input-group.contact__input
					.input-group-addon
						i.fa.fa-mobile.fa-lg
					input.form-control(type="text" id="InputPhone" placeholder="+7 978 657 75 64" ng-model="user.tel" value="{{user.tel}}")
		.image-column
			b Изображение
			img(ng-hide="hide" ng-src="./uploads/{{user.img}}" alt="Загрузка...")
			.form-group
				input(type="file" nv-file-select="" uploader="uploader" filtres="imageFilter")
			p(ng-hide="true") {{(uploader.queue[0].file.name)?user.img = uploader.queue[0].file.name: user.img = user.img ; hide = false}}
		.button-group
			button.btn.btn-success(type="submit" ng-click="contactsEdit(user);uploader.queue[0].upload()") Сохранить
			button.btn.btn-danger(type="submit" ng-click="contactsDel()") Удалить контакт
